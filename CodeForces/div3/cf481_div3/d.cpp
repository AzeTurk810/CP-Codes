/*
Telebe of Adicto && Mamedov yani AzeTurk810
I see humans but no humanity
*/
#include <iostream>
#include <utility>
#include <vector>

using ll = long long;
using namespace std;

#define ln '\n'
#define INFi 1e9
#define INFll 1e18

vector<pair<int, int>> v = {{1 , 0} , {0 , 0} , {1 , 1} , {0 , 1} , {-1 , }}

void solve() {
    int n;
    cin >> n;
    vector<int> b(n);
    for(int i = 0;i < n;i++) {
        cin >> b[i];
    }
    for(int i = 0;i < n;i++) {
        ;
    }
}

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    int t = 1;
//    cin >> t;
    for(int cases = 0 ; cases < t;cases ++) {
        solve();
    }
}
// Just Imaginary
